export interface IUser {
  firstName: string;
  id: string;
  lastName: string;
  picture: string;
  title: string;
}

export interface IUserInfo {
  id: string;
  dateOfBirth: string;
  email: string;
  firstName: string;
  picture: string;
  lastName: string;
  gender: string;
  phone: string;
  registerDate: string;
  title: string;
}